План Реализации
Backend Solution Skeleton
Цель: создать Clean Architecture основу.
Создать: server/GenesysForge.sln, проекты Domain, Application, Infrastructure, WebApi, Contracts, tests/*. Изменить: нет.
Backend: зависимости ASP.NET Core, EF Core, MediatR, FluentValidation. Frontend: нет.
Тесты: пустой тестовый проект собирается.
Готово: dotnet build проходит.

Frontend Vite Skeleton
Цель: создать React-приложение.
Создать: client/src/app, client/src/features, client/src/api, client/src/stores. Изменить: package.json, vite.config.ts.
Backend: нет. Frontend: React Router, TanStack Query, Zustand, RHF.
Тесты: базовый smoke test.
Готово: dev server открывает стартовый экран.

Shared Contracts
Цель: задать DTO для API.
Создать: Contracts/Auth, Contracts/Characters, Contracts/Rules, Contracts/Validation, Contracts/Pdf. Изменить: WebApi references.
Backend: request/response contracts. Frontend: TypeScript типы-зеркала в client/src/api/types.ts.
Тесты: build contracts.
Готово: backend/frontend используют единые формы DTO.

PostgreSQL + EF Core Setup
Цель: подключить БД и миграции.
Создать: Infrastructure/Persistence/AppDbContext.cs, Infrastructure/Persistence/Configurations/*. Изменить: appsettings*.json, Program.cs.
Backend: DbContext, connection string, migrations. Frontend: нет.
Тесты: DbContext factory test.
Готово: первая миграция создается и применяется.

Auth Domain + Persistence
Цель: модель пользователя.
Создать: Domain/Users/User.cs, Infrastructure/Auth/*. Изменить: AppDbContext.
Backend: user entity, password hash service или ASP.NET Identity adapter. Frontend: нет.
Тесты: password hashing test.
Готово: пользователь сохраняется в БД.

Register/Login API
Цель: регистрация и вход.
Создать: Application/Auth/Register, Application/Auth/Login, WebApi/Controllers/AuthController.cs. Изменить: Program.cs.
Backend: JWT/cookie auth, FluentValidation. Frontend: API client methods.
Тесты: integration tests для register/login.
Готово: можно получить auth token/session.

Auth UI
Цель: экраны входа и регистрации.
Создать: features/auth/LoginPage.tsx, RegisterPage.tsx, authApi.ts, authStore.ts. Изменить: router.
Backend: нет. Frontend: формы RHF, сохранение сессии.
Тесты: form validation tests.
Готово: пользователь входит и попадает в приложение.

Ruleset Tables + Seed Demo Data
Цель: база для rules-driven данных.
Создать: entities Ruleset, SourceBook, RuleSourceVersion, RuleEntity, RuleDefinition. Изменить: DbContext/configurations.
Backend: seed demo ruleset без защищенного контента. Frontend: нет.
Тесты: seed data exists.
Готово: в БД есть test ruleset/source/version/entities.

Rules API
Цель: отдавать доступные rulesets и справочники.
Создать: Application/Rules/GetRulesets, GetRuleCatalog, RulesController.cs. Изменить: нет.
Backend: queries через MediatR. Frontend: rulesApi.ts.
Тесты: API returns demo catalog.
Готово: frontend может загрузить ruleset list.

Ruleset Selection UI
   Цель: экран выбора ruleset/source.
   Создать: features/rules/RulesetSelect.tsx, useRulesets.ts. Изменить: wizard route shell.
   Backend: нет. Frontend: TanStack Query list.
   Тесты: loading/error/empty states.
   Готово: пользователь выбирает demo ruleset.

Character Core Domain
   Цель: базовая модель персонажа.
   Создать: Domain/Characters/Character.cs, CharacterSnapshot.cs, XpLedgerEntry.cs, CharacterSkill.cs, CharacterTalent.cs. Изменить: DbContext.
   Backend: aggregate methods без правил. Frontend: нет.
   Тесты: domain tests для создания draft.
   Готово: draft character создается в памяти и БД.

Character CRUD API
   Цель: список, создание, чтение персонажа.
   Создать: Application/Characters/Create, GetById, ListMine, CharactersController.cs. Изменить: auth policies.
   Backend: owner-scoped queries. Frontend: charactersApi.ts.
   Тесты: нельзя читать чужого персонажа.
   Готово: API CRUD draft работает.

Character List UI
   Цель: список персонажей пользователя.
   Создать: features/characters/CharacterListPage.tsx, CharacterCard.tsx. Изменить: navigation.
   Backend: нет. Frontend: list, empty state, create button.
   Тесты: renders empty/list.
   Готово: пользователь видит своих персонажей.

Creation Wizard Shell
   Цель: общий мастер создания.
   Создать: features/creation-wizard/WizardPage.tsx, wizardStore.ts, steps/*. Изменить: router.
   Backend: нет. Frontend: Zustand для текущего шага, RHF shell.
   Тесты: переходы вперед/назад.
   Готово: мастер сохраняет локальное состояние между шагами.

Basic Info Step
   Цель: имя и базовые данные персонажа.
   Создать: steps/BasicInfoStep.tsx. Изменить: create command DTO.
   Backend: validation name/ruleset. Frontend: RHF form.
   Тесты: required fields.
   Готово: draft создается с именем и ruleset.

Species/Archetype Step
   Цель: выбор species/archetype из конфигурации.
   Создать: steps/SpeciesStep.tsx, backend command SetCharacterSpecies. Изменить: CharactersController.
   Backend: проверка source/ruleset. Frontend: catalog selection.
   Тесты: нельзя выбрать entity из другого ruleset.
   Готово: выбор сохраняется.

Career Step
   Цель: выбор карьеры.
   Создать: steps/CareerStep.tsx, command SetCharacterCareer. Изменить: character model if needed.
   Backend: сохраняет career entity. Frontend: список careers.
   Тесты: invalid career rejected.
   Готово: карьера назначается персонажу.

Rule Snapshot Service
   Цель: фиксировать правила персонажа.
   Создать: Application/Rules/IRuleSnapshotService.cs, Infrastructure/Rules/RuleSnapshotService.cs. Изменить: character creation flow.
   Backend: snapshot active source versions/entities/rules. Frontend: нет.
   Тесты: snapshot не меняется после seed update.
   Готово: персонаж имеет rules snapshot.

Calculation Engine v1
   Цель: считать derived stats и XP totals из конфигов.
   Создать: Domain/Rules/CalculationEngine.cs, CalculatedCharacterStats.cs. Изменить: character query response.
   Backend: available/spent XP, basic derived stats from JSON formulas. Frontend: нет.
   Тесты: demo formulas.
   Готово: API возвращает calculated stats.

Rule Engine v1
   Цель: валидировать персонажа.
   Создать: Domain/Rules/RuleEngine.cs, ValidationResult.cs, Application/Characters/ValidateCharacter. Изменить: controller.
   Backend: requirements/cost/limits минимального DSL. Frontend: validationApi.ts.
   Тесты: валидный/невалидный demo character.
   Готово: API возвращает errors/warnings.

Validation UI Panel
   Цель: показывать ошибки и предупреждения.
   Создать: features/sheet/ValidationPanel.tsx. Изменить: wizard final step.
   Backend: нет. Frontend: TanStack Query validation.
   Тесты: renders errors/warnings.
   Готово: пользователь видит, что мешает завершить создание.

Skills Step + Career Skill Flags
   Цель: выбор стартовых/покупаемых навыков.
   Создать: steps/SkillsStep.tsx, command UpdateCharacterSkills. Изменить: Calculation Engine.
   Backend: ranks, career/non-career flag from career config. Frontend: skill table.
   Тесты: max ranks/career flag.
   Готово: навыки сохраняются и валидируются.

Dice Pool Calculator
   Цель: расчет пула навыка.
   Создать: Domain/Dice/DicePoolCalculator.cs, Application/Characters/GetSkillDicePools. Изменить: character detail response.
   Backend: characteristic + ranks -> structured dice pool. Frontend: dice display component.
   Тесты: matrix tests для demo values.
   Готово: каждый skill показывает pool breakdown.

Talents Step + Requirements
   Цель: покупка талантов с требованиями.
   Создать: steps/TalentsStep.tsx, command PurchaseTalent. Изменить: Rule Engine.
   Backend: validates tier/requirements/XP via config. Frontend: talent list and errors.
   Тесты: нельзя купить без требований/XP.
   Готово: talent purchase попадает в ledger.

Equipment Step
   Цель: покупка/экипировка предметов.
   Создать: steps/EquipmentStep.tsx, command UpdateEquipment. Изменить: Calculation Engine for encumbrance/defense/soak hooks.
   Backend: equipment inventory. Frontend: item list/cart.
   Тесты: quantity/equipped state.
   Готово: предметы сохраняются и влияют на расчеты там, где задано конфигом.

Finalize Creation
   Цель: перевести draft в active.
   Создать: command FinalizeCharacterCreation. Изменить: wizard final step.
   Backend: запускает validation, создает creation snapshot. Frontend: finish action.
   Тесты: invalid draft cannot finalize.
   Готово: активный персонаж открывается в листе.

Interactive Character Sheet
   Цель: основной лист персонажа.
   Создать: features/sheet/CharacterSheetPage.tsx, StatsPanel.tsx, SkillsPanel.tsx, TalentsPanel.tsx, EquipmentPanel.tsx. Изменить: routes.
   Backend: character detail endpoint includes calculated stats. Frontend: read-only MVP sheet.
   Тесты: sheet renders loaded character.
   Готово: лист показывает базу, derived stats, skills, dice pools.

XP Grant/Spend Ledger API
   Цель: развитие персонажа после создания.
   Создать: commands GrantXp, SpendXp, IncreaseSkillRank. Изменить: XP ledger model.
   Backend: ledger-based XP, validation before spend. Frontend: xpApi.ts.
   Тесты: spent XP cannot exceed available.
   Готово: XP начисляется и тратится через API.

XP UI
   Цель: экран/панель траты XP.
   Создать: features/sheet/XpPanel.tsx, SpendXpDialog.tsx. Изменить: CharacterSheetPage.
   Backend: нет. Frontend: grant XP, buy skill/talent actions.
   Тесты: mutation invalidates sheet query.
   Готово: после траты XP лист обновляется.

PDF Export Backend
   Цель: серверная генерация PDF.
   Создать: Application/Pdf/ExportCharacterPdf, Infrastructure/Pdf/*, PdfController.cs, template file. Изменить: DI.
   Backend: HTML/view model -> PDF, uses snapshot/current state. Frontend: API method.
   Тесты: export returns non-empty PDF.
   Готово: endpoint скачивает PDF.

PDF Export UI
   Цель: кнопка экспорта.
   Создать: features/pdf/ExportPdfButton.tsx. Изменить: CharacterSheetPage.
   Backend: нет. Frontend: download flow, loading/error.
   Тесты: button calls endpoint.
   Готово: пользователь скачивает PDF.

End-to-End MVP Flow
   Цель: проверить полный сценарий.
   Создать: tests/e2e/mvp-character-flow.spec.ts или интеграционный smoke. Изменить: test config.
   Backend: test seed. Frontend: test route flow.
   Тесты: register -> create -> finalize -> sheet -> XP -> PDF.
   Готово: один автоматический сценарий покрывает MVP.